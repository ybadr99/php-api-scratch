<?php

    // require_once 'config/Database.php';
    // require_once 'models/Post.php';


    // $db  = new Database;
    // $pdo = $db->connect();
    // $post = new Post($pdo);
    // var_dump($post->read()->fetchAll());